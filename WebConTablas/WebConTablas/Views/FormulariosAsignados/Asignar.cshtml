@{
    var formularios = ViewBag.Formularios as List<WebConTablas.Models.Formulario>;
    var pacientes = ViewBag.Pacientes as List<WebConTablas.Models.Paciente>;
}

<h2>Asignar Formulario a Paciente</h2>

@if (ViewBag.Errores != null)
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var err in ViewBag.Errores as List<string>)
            {
                <li>@err</li>
            }
        </ul>
    </div>
}

<form asp-action="Asignar" method="post">
    <div class="form-group">
        <label>Formularios</label>
        <div>
            @foreach (var f in formularios)
            {
                <div class="form-check">
                    <input type="checkbox" name="ID_Formulario" value="@f.ID_Formulario" class="form-check-input" id="formulario_@f.ID_Formulario" />
                    <label class="form-check-label" for="formulario_@f.ID_Formulario">@f.Titulo</label>
                </div>
            }
        </div>
    </div>
    <div class="form-group">
        <label for="ID_Paciente">Paciente</label>
        <select name="ID_Paciente" class="form-control" required>
            <option value="">Seleccione un paciente</option>
            @foreach (var p in pacientes)
            {
                <option value="@p.ID_Paciente">@p.Nombre</option>
            }
        </select>
    </div>
    <div class="form-group">
        <label for="Fecha_Limite">Fecha Límite (opcional)</label>
        <input type="date" name="Fecha_Limite" class="form-control" />
    </div>
    <button type="submit" class="btn btn-primary mt-2">Asignar</button>
</form>