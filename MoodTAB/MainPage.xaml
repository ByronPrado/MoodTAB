<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:MoodTAB.ViewModel" 
             x:Class="MoodTAB.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label
                Text="{Binding Title}"
                SemanticProperties.HeadingLevel="Level1" />
            <Entry
                Text="{Binding NuevaPersona}"
                Placeholder="Ingresa nombre"
            />
            <Entry
                Text="{Binding NuevoRut}"
                Placeholder="Ingresa rut"
            />
            <Button
                Text="Agregar" 
                Command="{Binding AddItemCommand}"
                HorizontalOptions="Fill" />

            <Button
                Text="Eliminar" 
                Command="{Binding DeleteDoneItemsCommand}"
                HorizontalOptions="Fill" 
                BackgroundColor="Red"/>

            <CollectionView ItemsSource="{Binding Items}">
                <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{Binding Nombre}" VerticalOptions="Center" />
                        <Label Text="{Binding Rut}" VerticalOptions ="Center" />
                        <CheckBox IsChecked="{Binding Done,Mode=TwoWay}" VerticalOptions="Center" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
